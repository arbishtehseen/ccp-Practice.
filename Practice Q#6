#include <stdio.h>
int main () {
    int num1 = 1 ;
    int num2 = 1; 
    int next = 2 , i ; 
     
     printf ("%d %d " , num1 , num2  ) ; 
     
     for (i=1 ; next <=500 ; i++) {
         
        next = num1 + num2;
        num1 = num2 ;
        num2 = next ; 
        
        if (next%5==0) {
            continue ; 
        }
        if (next >300) {
            break ; 
        }
        printf (" %d " , next) ; 
         
     }
}
